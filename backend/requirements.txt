# torch's default index resolves the huge CUDA build even on CPU-only machines
# and can hang for a very long time on a slow connection. Install the small
# CPU-only wheel first, then the rest:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
fastapi
uvicorn[standard]
python-multipart
groq
edge-tts
pypdf
python-docx
python-pptx
sentence-transformers
chromadb
sqlalchemy
pydantic
python-dotenv
